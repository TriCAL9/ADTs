package com.whiteboard.number;

import com.whiteboard.number.fraction;
import static java.util.Scanner.in;
public class RandomCode {
  private Fraction theoremGuass = Fraction.newInstance(Fraction.ONE, Fraction.newInstance(in.next(), in.next()));
}
